package com.eventticketing.reservation.domain;

/**
 * Seat status as seen by the mobile client on the live seat map.
 */
public enum SeatAvailabilityStatus {
    AVAILABLE,
    RESERVED,
    BOOKED
}
